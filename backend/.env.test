DATABASE_URL=postgresql://gwoodroof@localhost/captable_test?host=/var/run/postgresql
JWT_SECRET=test-jwt-secret-not-for-production
JWT_EXPIRATION=24h
PORT=3001
NODE_ENV=test
FRONTEND_URL=http://localhost:3000
DOMAIN=test.captable
APP_NAME=Cap Table Test
# Postmark sandbox mode restricts delivery to @getcaptable.com only, so email is disabled
# in tests. Verification tokens are read from the PendingRegistration DB table instead.
# Set this to the real key once the Postmark account is approved to enable maildrop.cc tests.
POSTMARK_API_KEY=
GOOGLE_CLIENT_ID=75301999161-o5ecmkc3grqughgds5i4vjn4ogpvrskh.apps.googleusercontent.com
